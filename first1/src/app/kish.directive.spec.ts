import { KishDirective } from './kish.directive';

describe('KishDirective', () => {
  it('should create an instance', () => {
    const directive = new KishDirective();
    expect(directive).toBeTruthy();
  });
});
